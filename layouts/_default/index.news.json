[
  {{- $pages := where site.RegularPages "Type" "!=" "json" -}}
  {{- $sortedPages := sort $pages "Date" "desc" -}}
  {{- range $index, $page := $sortedPages -}}
    {{- if $index }},{{ end -}}
    "{{ $page.RelPermalink }}"
  {{- end -}}
]
